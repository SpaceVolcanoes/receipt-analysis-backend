package a_theory.question2;

public class Gradle {

    //todo
    // Since lesson 1 we've been using Gradle.
    // So, you think you know gradle?

    //todo p1
    // In your words (do not use wiki definitions)
    // What is gradle?
    // Answer: Java dependency management system

    //todo p2
    // What is gradle wrapper? Why are we using it instead of gradle?
    // Answer: its a script which allows to use a fixed version of gradle without installing it on the machine,
    // having the same version in turn helps to avoid issues with project setup

    //todo p3
    // Name at least 1 alternative to gradle
    // 1 - maven

    //todo p4
    // Name 3 different benefits or reasons for using gradle
    // 1 - it helps to define which other external libraries our project wants to use and fetches them from the internet
    // 2 - it allows us to specify how we want to build our jar
    // 3 - we can use gradle to run our tests conveniently

}
