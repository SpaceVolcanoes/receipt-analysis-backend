package b_theory.question9;

public class Docker {

    //todo A
    // What is a server?
    // Answer: A piece of hardware that provides functionality to other devices.

    //todo B
    // What is the difference between build server and production server?
    // Answer: Build server, as the name implies, is generally for building projects. A production server is where your application will get hosted.

    //todo C
    // What is docker?
    // Answer: Docker is a software used for building containers for applications. These containers pack their own software and only share the underlying OS.

    //todo D
    // Name and explain docker container benefits over virtual machine setup (java jar as system process and installed nginx)
    // 1 Containers provide a consistent environment for the application to run in.
    // 2 Containers require less memory.

    //todo E
    // Name and explain docker container drawback over virtual machine setup (java jar as system process and installed nginx)
    // 1 Having a container setup makes updating applications slower. Impacts smaller fixes the most.

    //todo F
    // Name and describe tools for docker architecture
    // 1 Docker Compose is a deployment tool which can manage the entire lifecycle of a Docker application.
    // 2 Prometheus is a monitoring tool which has also been developed to work with docker.

    //todo G
    // Name and explain why are companies slow in moving existing systems to docker architecture (do not explain why docker is bad)
    // 1 Some organisations might be struggling to adapt to any changes due to the style of management in said organisation.
    // 2 The organisation might not have enough people with experience that can go through with moving to docker.
}
