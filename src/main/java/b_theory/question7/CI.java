package b_theory.question7;

public class CI {

    //todo A
    // What is integration?
    // Answer: In a general IT context, it is the act of connecting several smaller systems into a larger system.
    // In the context of developing, it is the act of combining a developers changes (to the code) with the code in the central repository of a project.

    //todo B
    // What makes integration continuous?
    // Answer: When integration is done often and automatic tests are run after integration.

    //todo C
    // In the class we had 3 stages in our CI/CD process, tick [x] which ones stand for continuous integration
    // [X] compile
    // [X] test
    // [] deploy

    //todo D
    // Name and explain 1 benefit of CI
    // 1 When bugs emerge, they are caused by a small set of changes, making them easier to track down.

    //todo E
    // Name and explain 1 drawback of CI
    // 1 Setting up tests and/or an automated build system takes time, which means that in a small project, it might not be worth it.

    //todo F
    // In the class we are using gitlab-ci. Name 2 alternatives to it
    // 1 solano ci
    // 2 bitbucket pipelines
}
